# **Networking for DevOps: Как сети реально работают в Docker, Kubernetes и облаке**

## Зачем DevOps вообще OSI?

DevOps не проектирует сети с нуля.  
DevOps чинит прод, где:

- Сервис не отвечает
    
- Pod не выходит в интернет
    
- Ingress отдаёт 502
    
- Healthcheck падает
    

**OSI для DevOps — это не теория, а карта диагностики.**

> ❗ Вопрос DevOps не «что такое уровень 3?»  
> ❗ Вопрос DevOps — «на каком уровне сломалось и **чем проверить?**»

---

## Как DevOps думает о сетевой проблеме

Когда сервис не работает, мы не гадаем — мы идём **по уровням сверху вниз**:

1. **L7** → HTTP / gRPC / DNS
    
2. **L6** → TLS / сертификаты
    
3. **L5** → Таймауты / сессии
    
4. **L4** → Порт / TCP / UDP
    
5. **L3** → IP / маршруты / NAT
    
6. **L2** → CNI / bridge / ARP
    
7. **L1** → Интерфейс / нода / облако
    

---

## OSI, как её видит DevOps

### 🟦 **L7 — Application (API, HTTP, gRPC)**

**Что ломается чаще всего:**

- 502 / 503 / 504 ошибки
    
- Неправильный healthcheck
    
- Ошибки в headers / CORS
    

**Инструменты:**

- `curl`, `wget`, `grpcurl`, Postman
    
- Ingress / API Gateway логи
    
- Loki / ELK (логи приложений)
    

---

### 🟪 **L6 — Presentation (TLS, шифрование)**

**Типовые проблемы:**

- `TLS handshake failed`
    
- Expired certificate
    
- CN / SAN mismatch
    

**Инструменты:**

- `curl -v`
    
- `openssl s_client`
    
- cert-manager
    
- Vault / Let’s Encrypt
    

---

### 🟨 **L5 — Session (таймауты, сессии)**

**Типовые проблемы:**

- Load Balancer рвёт соединения
    
- WebSocket падает
    
- Idle timeout
    

**Инструменты:**

- NGINX / HAProxy / Envoy конфиги
    
- Keepalive / timeout настройки
    
- Service Mesh (Istio, Linkerd)
    

---

### 🟥 **L4 — Transport (TCP / UDP, порты)**

**🔥 Критично для DevOps**

**Типовые проблемы:**

- Порт закрыт
    
- `SYN_SENT` (висит подключение)
    
- `connection refused`
    

**Инструменты:**

- `ss -lntup`
    
- `netstat`
    
- `nc`
    
- `tcpdump port 443`
    
- Kubernetes Service / NodePort
    

---

### 🟩 **L3 — Network (IP, маршруты)**

**Типовые проблемы:**

- Pod не выходит в интернет
    
- Нет маршрута до сервиса
    
- Сломан NAT
    

**Инструменты:**

- `ip route`
    
- `ping`
    
- `traceroute`, `mtr`
    
- `iptables` / `nftables`
    
- Cloud VPC routing tables
    

---

### 🟦 **L2 — Data Link (CNI, MAC, bridge)**

**Типовые проблемы:**

- Pod не видит другой pod
    
- Проблемы с CNI плагином
    
- ARP конфликт
    

**Инструменты:**

- `ip link`
    
- `ip neigh` (ARP таблица)
    
- `bridge`
    
- `tcpdump -e` (смотрим MAC)
    
- CNI: Calico / Flannel / Cilium
    

---

### 🟫 **L1 — Physical (ноды, облако)**

**Типовые проблемы:**

- Нода недоступна
    
- Интерфейс `down`
    
- Облачный инцидент (AWS/Azure/GCP)
    

**Инструменты:**

- `ip a`
    
- `ethtool`
    
- Cloud Console (статус VM)
    
- Node health checks (kubelet)
    

---

## OSI vs реальный стек DevOps

|Уровень OSI|DevOps реальность|
|---|---|
|L7|HTTP, gRPC, Ingress, API Gateway|
|L6|TLS, Сертификаты, cert-manager|
|L5|Load Balancer, Service Mesh|
|L4|Ports, Kubernetes Service, TCP/UDP|
|L3|Pod IP, VPC CIDR, маршруты|
|L2|CNI (Calico, Cilium), bridge, veth|
|L1|Node, Cloud VM, сетевой интерфейс|

---

## Kubernetes через призму OSI

|Уровень|Что это в Kubernetes|
|---|---|
|L7|Ingress, Gateway API, Service Mesh (Istio)|
|L6|cert-manager, TLS termination в Ingress|
|L5|kube-proxy (session affinity), Service Mesh|
|L4|Service (ClusterIP, NodePort, LoadBalancer)|
|L3|Pod IP (CIDR), NetworkPolicy, CNI маршрутизация|
|L2|veth pair, CNI плагин (bridge, ipvlan, macvlan)|
|L1|Нода, сетевая карта, облачный instance|

---

## Главная мысль для DevOps

**OSI — это не модель сетей.  
OSI — это модель поиска проблемы.**

Если DevOps понимает:

1. **Какой уровень** сейчас проверяет
    
2. **Какими инструментами** его диагностировать
    
3. **Какие симптомы** типичны для этого уровня
    

→ он быстро чинит прод, **даже не зная архитектуру заранее.**

---

## Практический чеклист при инциденте

1. **L7**: `curl -I https://service` → 502?
    
2. **L6**: `openssl s_client -connect service:443` → сертификат валиден?
    
3. **L4**: `nc -zv service 443` → порт открыт?
    
4. **L3**: `ping pod-ip` → есть ли связность?
    
5. **L2**: `kubectl describe pod` → CNI выдал IP?
    
6. **L1**: `kubectl get nodes` → нода Ready?
    

---

## Резюме

- **Docker**: bridge-сети, порты, DNS
    
- **Kubernetes**: CNI, Services, Ingress, Network Policies
    
- **Облако**: VPC, Security Groups, Load Balancers, NAT Gateway
    

**Ваш инструмент — не теория, а методология:**  
От уровня к уровню, от симптома к причине, от проверки к исправлению.

---

> **Networking for DevOps** — это не про проектирование.  
> Это про диагностику, исправление и автоматизацию того, что уже работает (или не работает) в продакшене.

# CCNA Мини-курс на основе Jeremy's Mega Lab
## Часть 1: Initial Setup (Начальная настройка)

---

## 📋 Теоретический минимум

### Что такое Initial Setup?
Это базовые настройки безопасности и управления, которые должны быть выполнены на **каждом** сетевом устройстве перед его вводом в эксплуатацию.

### Ключевые концепции:

**1. Hostname (Имя устройства)**
- Уникальное имя для идентификации устройства
- Отображается в CLI prompt
- Помогает ориентироваться в сети

**2. Enable Secret**
- Пароль для доступа в привилегированный режим (Privileged EXEC mode)
- Хешируется для безопасности
- Type 5 (MD5) vs Type 9 (scrypt) - scrypt более безопасен

**3. User Account**
- Локальная учетная запись на устройстве
- Используется для аутентификации при входе
- Альтернатива паролям линий

**4. Console Line Configuration**
- Настройка консольного порта (физическое подключение)
- Login Local - требует локальную учетную запись
- Exec-timeout - автоматический выход после бездействия
- Logging Synchronous - улучшает читаемость в CLI

---

## 🎯 Цели раздела

По завершении этой части вы сможете:
- ✅ Настроить hostname на всех устройствах
- ✅ Защитить привилегированный режим паролем
- ✅ Создать локальные учетные записи
- ✅ Настроить безопасный доступ через консольный порт
- ✅ Сохранять конфигурации в startup-config

---

## 🛠️ Практическая часть

### Топология
В этой части работаем со всеми устройствами:
- **R1** (маршрутизатор)
- **CSW1, CSW2** (core switches)
- **DSW-A1, DSW-A2, DSW-B1, DSW-B2** (distribution switches)
- **ASW-A1, ASW-A2, ASW-A3** (access switches Office A)
- **ASW-B1, ASW-B2, ASW-B3** (access switches Office B)

---

## 📝 Пошаговая инструкция

### ⚠️ ВАЖНО: Перед началом
Скачайте лабораторный файл (.pka) с сайта Jeremy's IT Lab или используйте свою топологию.

---

### Шаг 1: Настройка Hostname на R1

```cisco
R1# enable
R1# configure terminal
R1(config)# hostname R1
R1(config)# exit
R1# write memory
```

**Объяснение:**
- `enable` - вход в привилегированный режим
- `configure terminal` (или `conf t`) - вход в режим глобальной конфигурации
- `hostname R1` - задаём имя устройства
- `write memory` (или `do write`, `copy run start`) - сохранение конфигурации

💡 **Совет**: Используйте `do write` для быстрого сохранения из режима конфигурации.

---

### Шаг 2: Настройка Enable Secret

**На R1 (поддерживает только Type 5):**
```cisco
R1(config)# enable secret jeremysitlab
R1(config)# do show run | include secret
```

**На CSW1 (поддерживает Type 9):**
```cisco
CSW1(config)# enable algorithm-type scrypt secret jeremysitlab
CSW1(config)# do show run | include secret
```

**Объяснение:**
- Type 5 (MD5) - старый стандарт хеширования
- Type 9 (scrypt) - современный, более безопасный
- В Packet Tracer: многослойные коммутаторы (3650) поддерживают Type 9
- Роутеры и access switches - только Type 5

---

### Шаг 3: Создание User Account

**На R1 (Type 5):**
```cisco
R1(config)# username cisco secret ccna
R1(config)# do show run | include username
```

**На CSW1 (Type 9):**
```cisco
CSW1(config)# username cisco algorithm-type scrypt secret ccna
CSW1(config)# do show run | include username
```

**Учетные данные для всех устройств:**
- Username: `cisco`
- Password: `ccna`

---

### Шаг 4: Настройка Console Line

**На всех устройствах:**
```cisco
R1(config)# line console 0
R1(config-line)# login local
R1(config-line)# exec-timeout 30 0
R1(config-line)# logging synchronous
R1(config-line)# exit
R1(config)# do write
```

**Объяснение команд:**
- `line console 0` - вход в настройку консольной линии
- `login local` - требовать локальную учетную запись
- `exec-timeout 30 0` - таймаут 30 минут (0 секунд)
- `logging synchronous` - логи не прерывают ввод команд

---

### Шаг 5: Настройка остальных устройств

**Используйте метод Copy-Paste для ускорения:**

**Для R1 и Access Switches (Type 5):**
```cisco
enable secret jeremysitlab
username cisco secret ccna
line console 0
 login local
 exec-timeout 30 0
 logging synchronous
```

**Для Core и Distribution Switches (Type 9):**
```cisco
enable algorithm-type scrypt secret jeremysitlab
username cisco algorithm-type scrypt secret ccna
line console 0
 login local
 exec-timeout 30 0
 logging synchronous
```

💡 **Совет в Packet Tracer**: 
1. Подготовьте команды в текстовом файле
2. Правый клик в терминале → Paste
3. Не забывайте `do write` после каждого устройства!

---

## ✅ Проверка конфигурации

### Проверьте каждое устройство:

**1. Проверка hostname:**
```cisco
R1# show running-config | include hostname
```
Должно отобразиться: `hostname R1`

**2. Проверка enable secret:**
```cisco
R1# show running-config | include secret
```
Вы увидите хешированный пароль (начинается с $1$ для Type 5 или $9$ для Type 9)

**3. Проверка user account:**
```cisco
R1# show running-config | section username
```

**4. Проверка console line:**
```cisco
R1# show running-config | section line con
```

**5. Проверка сохранения:**
```cisco
R1# show startup-config | include hostname
```
Если видите hostname - конфигурация сохранена!

---

## 🎓 Тест входа

**Проверьте работу аутентификации:**

1. Выйдите из устройства: `exit` (несколько раз)
2. Попробуйте войти снова через консоль
3. Введите username: `cisco`
4. Введите password: `ccna`
5. Войдите в privileged mode: `enable`
6. Введите enable secret: `jeremysitlab`

✅ Если всё работает - настройка выполнена правильно!

---

## 📊 Таблица устройств для настройки

| Устройство | Hostname | Enable Secret Type | Команды |
|------------|----------|-------------------|---------|
| R1 | R1 | Type 5 (MD5) | `enable secret` |
| CSW1 | CSW1 | Type 9 (scrypt) | `enable algorithm-type scrypt secret` |
| CSW2 | CSW2 | Type 9 (scrypt) | `enable algorithm-type scrypt secret` |
| DSW-A1 | DSW-A1 | Type 9 (scrypt) | `enable algorithm-type scrypt secret` |
| DSW-A2 | DSW-A2 | Type 9 (scrypt) | `enable algorithm-type scrypt secret` |
| DSW-B1 | DSW-B1 | Type 9 (scrypt) | `enable algorithm-type scrypt secret` |
| DSW-B2 | DSW-B2 | Type 9 (scrypt) | `enable algorithm-type scrypt secret` |
| ASW-A1 | ASW-A1 | Type 5 (MD5) | `enable secret` |
| ASW-A2 | ASW-A2 | Type 5 (MD5) | `enable secret` |
| ASW-A3 | ASW-A3 | Type 5 (MD5) | `enable secret` |
| ASW-B1 | ASW-B1 | Type 5 (MD5) | `enable secret` |
| ASW-B2 | ASW-B2 | Type 5 (MD5) | `enable secret` |
| ASW-B3 | ASW-B3 | Type 5 (MD5) | `enable secret` |

---

## 💡 Практические советы

### Ускорение работы в Packet Tracer:
1. **Notepad метод**: Подготовьте все команды в блокноте
2. **Copy-Paste**: Используйте правый клик → Paste в CLI
3. **Групповая настройка**: Сначала все Type 5, потом все Type 9
4. **Не забывайте сохранять**: `do write` после каждого устройства

### Типичные ошибки:
❌ Забыть `do write` - конфигурация потеряется при перезагрузке
❌ Неправильный тип хеширования для устройства
❌ Опечатки в паролях (регистр важен!)
❌ Неправильный hostname (должен совпадать с топологией)

---

## 🔄 Режимы Cisco IOS

Запомните иерархию режимов:

```
User EXEC Mode (>)
    ↓ enable
Privileged EXEC Mode (#)
    ↓ configure terminal
Global Configuration Mode (config)#
    ↓ line console 0
Line Configuration Mode (config-line)#
```

**Выход:**
- `exit` - на один уровень вверх
- `end` - сразу в Privileged EXEC Mode
- `Ctrl+Z` - то же, что `end`

---

## 📚 Ключевые команды для запоминания

```cisco
# Базовые
enable                          # Вход в privileged mode
configure terminal              # Вход в config mode
hostname [name]                 # Задать имя устройства
exit                           # Выход на уровень вверх
end                            # Выход в privileged mode

# Безопасность
enable secret [password]                        # Type 5
enable algorithm-type scrypt secret [password]  # Type 9
username [name] secret [password]               # Type 5 user
username [name] algorithm-type scrypt secret [password]  # Type 9 user

# Console Line
line console 0                 # Настройка консоли
login local                    # Локальная аутентификация
exec-timeout [min] [sec]       # Таймаут
logging synchronous            # Синхронные логи

# Сохранение и просмотр
write memory / write / copy run start  # Сохранить конфигурацию
show running-config            # Текущая конфигурация
show startup-config            # Сохраненная конфигурация
show running-config | include [text]  # Фильтр вывода
show running-config | section [text]  # Показать секцию
```

---

## 🎯 Чек-лист завершения Части 1

Отметьте выполненные пункты:

- [ ] Настроен hostname на всех 13 устройствах
- [ ] Настроен enable secret (Type 5 или Type 9) на всех устройствах
- [ ] Создана учетная запись cisco/ccna на всех устройствах
- [ ] Настроена console line на всех устройствах
- [ ] Все конфигурации сохранены (do write)
- [ ] Проверена аутентификация (вход через консоль)
- [ ] Проверена работа enable secret
- [ ] В Packet Tracer файле процент выполнения увеличился

---

## 🚀 Готовы к Части 2?

В следующей части мы рассмотрим:
- **VLANs** - создание и назначение
- **Trunks** - магистральные каналы
- **EtherChannel** - агрегация каналов (Layer 2)
- **VTP** - протокол транкинга VLAN
- **Port Security** - начальная настройка портов

**Увидимся в Части 2! 🎓**

---

## 📖 Дополнительные ресурсы

- Jeremy's IT Lab CCNA Course (YouTube)
- Cisco IOS Command Reference
- Packet Tracer Activities

**Вопросы?** Повторите материал и переходите к практике! Только практика делает мастера! 💪